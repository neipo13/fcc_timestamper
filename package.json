{
  "name": "timestamper",
  "version": "0.1.0",
  "description": "gives both unix and string timestamps when given either",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew Napierkowski",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0",
    "moment": "^2.15.2"
  }
}
